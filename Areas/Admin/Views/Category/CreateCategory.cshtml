
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Movie</title>
    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .create-movie-card {
            max-width: 650px;
            margin: 50px auto;
            background: #ffffff;
            border-radius: 20px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            padding: 30px;
        }

        .form-label {
            font-weight: 600;
            color: #333;
        }

        .btn-submit {
            width: 100%;
            background: #007bff;
            color: #fff;
            font-weight: bold;
            border-radius: 50px;
            padding: 12px;
            transition: 0.3s ease;
        }

            .btn-submit:hover {
                background: #0056b3;
                transform: scale(1.03);
            }

        h2 {
            font-weight: bold;
            margin-bottom: 25px;
            text-align: center;
            color: #333;
        }
    </style>
</head>
<body>

    <div class="container">
        <div class="create-movie-card">
            <h2>Create New Category</h2>
            <form method="post">
                <!-- Movie Name -->
                <div class="mb-3">
                    <label for="movieName" class="form-label">Category Name</label>
                    <input type="text" class="form-control" id="movieName" placeholder="Enter movie name" name="Name">
                </div>
                @* <span class="text-danger" asp-validation-for="@Model.Movies.Name"></span> *@


                <!-- Submit Button -->
                <button type="submit" class="btn btn-submit">Submit</button>
            </form>
        </div>
    </div>

</body>
</html>
